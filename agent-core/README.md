# wattson-agent-core

Poetry-managed Python package for WATTSON’s AI layer:
- Autogen multi-agent orchestration
- Deterministic tools (ontology, schema, diff budget)
- CLI shim for local runs

## Dev quickstart
```bash
cd agent-core
poetry install
poetry run wattson-agent --help
